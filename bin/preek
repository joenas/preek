#!/usr/bin/env ruby

require "preek"

unless ARGV.empty?
  action = ARGV.shift.to_sym
  preek = Preek::Preek.new
  if preek.respond_to? action
    preek.send(action, ARGV[0])
  else
    preek.parse action
  end
else
  Preek::Preek.start
end
